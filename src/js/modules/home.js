const homeContent = (() => {
  const content = '<h2>Welcome to our intangible restaurant</h2><h4>You can ask for the <span class="code">menu()</span> over there, in the <span class="code">developer console</span></h4>';
  return content;
});

export default homeContent